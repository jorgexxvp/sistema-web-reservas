// REQUEST

// RESPONSE
export interface IRoleResponse {
  list: {
    codigo: string;
    id: number;
    nombre: string;
  }[];
}

export interface IDocumentResponse {
  list: {
    codigo: string;
    id: number;
    nombre: string;
  }[];
}
